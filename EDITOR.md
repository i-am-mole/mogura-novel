# もぐらノベル エディタ

## 目的

GitHub Pagesで公開する `private/` 配下のMarkdownを、既存形式と不要な差分を維持しながら編集するWindows向けGUIエディタです。

自己紹介、作品情報、各話を日本語フォームから編集できます。保存時には追跡対象外の一時領域でSSGを実行し、通常のWebブラウザで公開イメージを確認できます。

## 必要な事前準備

- Windows
- Python 3.11以降
- このリポジトリのローカルコピー

初回起動時に `.venv` がなければ、利用可能なPythonを探して仮想環境を自動作成し、`requirements.txt` のライブラリをインストールします。

## 起動方法

エクスプローラーでリポジトリを開き、`start_editor.bat` をダブルクリックします。

コマンドプロンプトから起動する場合:

```bat
start_editor.bat
```

## 基本的な使い方

左側の一覧から次を選択します。

- 自己紹介
- 作品情報
- 章ごとに分類された各話

右側に選択したファイルの編集欄が表示されます。ウィンドウを広げると、左側の一覧より編集領域が優先して広がります。

画面下には保存状態と、現在編集中のファイル全体の文字数が常に表示されます。文字数は改行を除外し、Markdown記号とルビ記法をそのまま数えた編集補助用の概算です。

タイトル、本文、タグ、リンク、章構成などの編集欄はArialで表示されます。画面上部の「編集フォント」から、Windowsにインストールされている別のフォントへ切り替えられます。ボタンや説明など、UI自体のフォントは変更されません。

## 作品の作成・編集

「新しい作品」を押し、次を入力します。

- `work-slug`
- タイトル
- タグ
- 連載ステータス
- あらすじ

作品情報画面では、タイトル、タグ、連載ステータス、あらすじ、外部リンク、章タイトルと章区切り番号を個別に編集できます。

slugは小文字の半角英数字を単一のハイフンで区切った名前に限定されます。`blog`、`wiki`、`css`、Windows予約名、既存名と重複する名前は使用できません。

作品ディレクトリの改名と作品全体の削除は、このバージョンでは扱いません。

## 各話の作成・編集

対象作品を選択して「新しい話」を押し、次を入力します。

- `episode-slug`
- タイトル
- 話数番号
- 本文

話数番号と章区切り番号に従い、左側の一覧は章・話数順に並びます。

各話を選択して「名前変更」を押すとファイル名を変更できます。このとき `data/update_history.csv` のキーも同時に変更し、従来の更新日時を保持します。

「削除」は対象を明記した確認画面を表示します。削除されたファイルは `.novel-editor/trash/` へ移され、「削除を元に戻す」で直前の1件を復元できます。

## ルビ入力補助

ルビを振る文字列を選択し、「ルビ」または `Ctrl+Shift+R` を押します。入力したルビにより、選択範囲を次の形式へ置き換えます。

```text
|対象文字列<ルビ>
```

自己紹介、作品タイトル、あらすじ、章タイトル、話タイトル、本文で利用できます。対象未選択、空のルビ、改行をまたぐ選択、不正な記号を含む入力は変更せずエラーを表示します。

## 保存とプレビュー

「保存」または `Ctrl+S` で保存します。変更されたフィールドだけを元のMarkdownへ戻し、関係のない空行、改行コード、末尾改行などは保持します。無変更保存ではファイルを書き直しません。

保存後、SSGをバックグラウンドで実行します。初回成功時に通常のWebブラウザを開き、以後は成功した生成に合わせてブラウザを自動更新します。

プレビューは `.novel-editor/preview-builds/` に生成されます。プレビューだけでは次の追跡ファイルを変更しません。

- `docs/`
- `data/update_history.csv`

通常の公開物を生成する場合は、従来どおり次を実行します。

```bat
.venv\Scripts\python.exe tools\publish.py
```

公開SSGはサイト全体を `.novel-editor/` 内へ生成し、全処理が成功した場合だけ `docs/` と更新履歴を入れ替えます。生成失敗時は既存の公開物を維持します。

## 一時領域

`.novel-editor/` はGit管理対象外で、次を保存します。

- プレビュー生成物
- 公開生成の一時ファイルとロールバック用バックアップ
- 保存前バックアップ
- 削除した話の一時ごみ箱
- テスト一時ファイルとPythonキャッシュ

必要な復元を終える前に、この領域を手動削除しないでください。

## エラー発生時

入力検証エラーやSSGエラーはエディタ上に表示されます。プレビュー生成が失敗しても、直前に成功したブラウザ表示は維持されます。

ファイルをエディタ外で変更した後に保存しようとした場合は上書きを停止します。左側の項目を選び直して再読み込みしてください。

## テスト

一時ファイルを必ずリポジトリ内へ置く専用ランナーを使用します。

```bat
.venv\Scripts\python.exe tools\run_tests.py
```

## 制限事項

- Windows専用です。
- 自動保存は行いません。
- 作品ディレクトリの改名・削除には対応しません。
- GUIの見た目とブラウザ起動は環境差があるため、リリース前に手動確認が必要です。
- `.novel-editor/` の削除後は、エディタ内ごみ箱からの復元はできません。
