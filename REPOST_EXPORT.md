# 小説転載用テキスト出力ツール

## 目的

`tools/repost_export.py` は、GitHub Pages 用の正本である
`private/<slug>/` の Markdown 原稿を、他の小説投稿サイトへコピーしやすい
プレーンテキストへ変換するコマンドラインツールです。

このツールが行うのはローカルテキストファイルの生成だけです。投稿サイトへの
ログイン、投稿、既存投稿の更新は自動化しません。

## 前提環境

- Windows PowerShell
- リポジトリのルートディレクトリ
- `requirements.txt` に従って作成した既存の `.venv`
- `private/<slug>/index.md` と各話 Markdown が既存SSGの検証を通ること

生成済みの `docs/` HTMLは読みません。既存の `Novel.load_if_valid()` で
`private/` の原稿を読み、各話の抽出、検証、`number` 順の並べ替えを行います。

## 実行方法

作品の全話を1話1ファイルで出力する例です。

```powershell
.venv\Scripts\python.exe tools\repost_export.py --slug kaminomori --to kakuyomu
```

作品タイトルとあらすじだけを出力する例です。

```powershell
.venv\Scripts\python.exe tools\repost_export.py --slug kaminomori --to narou --taiara
```

### 引数

| 引数 | 必須 | 内容 |
| --- | --- | --- |
| `--slug` | 必須 | `private/<slug>/` の単一ディレクトリ名 |
| `--to` | 必須 | `narou`、`kakuyomu`、`pixiv`、`hameln` のいずれか |
| `--taiara` | 任意 | 指定時はタイトル・あらすじモード、未指定時は全話モード |

`--slug` に絶対パス、`.`、`..`、パス区切り文字を含む値は指定できません。
シンボリックリンクなどによって `private/` の外を参照する値も拒否します。

## モード

### `--taiara` あり

`title-outline.txt` を1ファイルだけ生成します。

```text
【タイトル】
<変換後の作品タイトル>

【あらすじ】
<変換後のあらすじ>
```

### `--taiara` なし

`Novel.stories` の順、すなわち原稿の `number` 順に、1話1ファイルを生成します。
ファイル名は `001.txt`、`002.txt`、`003.txt` の形式です。総話数が1000以上なら
桁数を増やすため、ファイル名の辞書順と話順が一致します。

```text
【タイトル】
<変換後の各話タイトル>

【本文】
<変換後の本文>
```

各話タイトルに「1話」などの表示用番号は追加しません。tags、status、
external links、chaptersも出力しません。

投稿サイトの入力欄へコピーするときは、位置を確認するための区切りである
`【タイトル】`、`【あらすじ】`、`【本文】` 自体は貼り付けないでください。

## サイト別のルビ変換

原稿の独自ルビ記法は `|親文字<ルビ>` です。

| 転載先 | 作品タイトル | あらすじ | 各話タイトル | 各話本文 |
| --- | --- | --- | --- | --- |
| 小説家になろう (`narou`) | `親文字（ルビ）` | `親文字（ルビ）` | `親文字（ルビ）` | `｜親文字《ルビ》` |
| カクヨム (`kakuyomu`) | `親文字（ルビ）` | `親文字（ルビ）` | `親文字（ルビ）` | `｜親文字《ルビ》` |
| pixiv (`pixiv`) | `親文字（ルビ）` | `親文字（ルビ）` | `親文字（ルビ）` | `[[rb: 親文字 > ルビ]]` |
| ハーメルン (`hameln`) | `親文字（ルビ）` | `｜親文字《ルビ》` | `｜親文字《ルビ》` | `｜親文字《ルビ》` |

ルビ非対応欄では読みを失わないよう、必ず `親文字（ルビ）` にします。

### サイト側の制限

- 小説家になろうの本文
  - 親文字とルビはそれぞれ1～10文字です。
  - 親文字またはルビに `&`、`"`、`<`、`>` が含まれる場合は
    `親文字（ルビ）` にフォールバックします。
- カクヨムの本文
  - 親文字は1～20文字、ルビは1～50文字です。
  - 改行をまたぐルビは `親文字（ルビ）` にフォールバックします。
- pixivの本文
  - 公式に確認できない文字数制限は設けていません。
- ハーメルンのルビ対応欄
  - 親文字とルビは通常それぞれ20文字以内です。
  - 文字列全体が半角英数字だけなら、それぞれ60文字以内です。
  - 20文字を超える混在文字列は `親文字（ルビ）` にフォールバックします。

問題のあるルビだけをフォールバックし、ほかの本文の変換は続けます。その際、
対象ファイル、対象文字列、理由を標準エラーへ警告として表示します。作品名、
あらすじ、本文をサイトの文字数制限に合わせて切り詰めることはありません。

## Markdownのプレーンテキスト化

SSGと同じ `md.to_html()` でMarkdownと独自ルビをHTMLへ変換した後、Python標準
ライブラリのHTMLパーサーで次のようにプレーンテキスト化します。

| Markdown / HTML | 出力 |
| --- | --- |
| 段落 | 段落間に空行 |
| 通常改行、`<br>` | 改行を維持 |
| 水平線 | `◇　◇　◇` |
| 箇条書き | 行頭に `・` |
| 番号付きリスト | 行頭に `1. `、`2. ` など |
| 引用 | 各行の先頭に `＞` |
| リンク | `表示文字（URL）`。表示文字とURLが同じなら重複させない |
| 太字、斜体、コード | 装飾を除き、文字列を残す |
| 見出し | 装飾を除き、文字列と適切な改行を残す |
| HTMLエンティティ | 人間が読める文字へ戻す |
| 未知のHTMLタグ | タグを除き、内部テキストを可能な限り残す |

先頭の字下げを含む全角スペースは維持します。行末空白と過剰な連続空行は
正規化し、ファイル末尾は1つの改行で終わります。出力はBOMなしUTF-8、
改行コードはOSにかかわらずLF (`\n`) です。

## 出力先

出力先は次の配下です。

```text
.novel-editor/repost-export/<slug>/<to>/<YYYYMMDD-HHMMSS>/
```

同じ秒に同じ条件で再実行した場合は `-001`、`-002` のような連番を付け、
既存出力を上書きしません。全ファイルを一時ディレクトリへ書き終えてから
最終ディレクトリへ移動するため、失敗した処理の途中ファイルは最終出力として
残りません。

`.novel-editor/` はリポジトリの `.gitignore` で除外されているため、出力
ファイルはGit追跡対象外です。

正常終了時は、標準出力へ対象slug、転載先、モード、出力先の絶対パス、
出力ファイル数、警告数を表示します。

## エラー時の確認事項

エラーになった場合は、標準エラーに表示された原因とパスを確認してください。

- `--slug` が `private/` 直下の単一ディレクトリ名になっているか
- `private/<slug>/index.md` が存在するか
- `index.md` と各話MarkdownがREADME記載の既存フォーマットを満たすか
- 通常モードの場合、対象作品に少なくとも1話あるか
- `.novel-editor/repost-export/` 配下へディレクトリとファイルを作成できるか
- `--to` が対応する4サイトのいずれかになっているか

既存バリデーションエラーがある場合は出力を作成せず、エラー一覧を表示して
非0の終了コードで終了します。
