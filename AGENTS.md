# リポジトリ作業指示

## タスクとリポジトリの把握

- タスクを指示されたら、まず `README.md` を読み、そこからリンクされた文書のうちタスクに関係するものを特定して参照すること。
- 変更前に、現在のリポジトリの状態と、関係する実装、テスト、文書を確認すること。リポジトリ内で確認できる事柄を推測で補わないこと。
- リポジトリ構成、環境構築、ブランチ命名、検証、公開手順は `DEVELOPMENT.md` に従うこと。`README.md` からリンクされた、より具体的な文書が該当する場合はそちらにも従うこと。
- サイトコンテンツは `private/` を正本として扱うこと。生成物である `docs/` と `data/update_history.csv` は直接編集せず、入力を変更して文書化された手順で再生成すること。

## Git 運用

- ユーザーから特に指定がない限り、変更前に `DEVELOPMENT.md` の命名規則に従った短命な作業ブランチを作成し、そのブランチへ切り替えること。
- ブランチの作成と切り替えについて、ユーザーへ承認を求めないこと。これらの通常の Git 操作を確認するためだけに作業を中断しないこと。
- ユーザーによる無関係な変更を保持し、一つのブランチには一つの論点だけを含めること。

## Python 環境とテスト

- プロジェクトのツールやテストを実行するときは、`venv.bat` によって作成されたリポジトリ内の Python 環境だけを使用すること。既存の `.venv/` はこの方法で作成された環境として再利用してよい。
- `.venv/` がない場合は、プロジェクトのツールやテストを実行する前に `venv.bat` を実行して作成すること。非対話で Python を実行するときは、使用環境を明確にするため `.venv\Scripts\python.exe` を直接呼び出すこと。
- ユーザーから特に指定がない限り、プロジェクトのツールとそのテストはユーザーへ承認を求めずに実行すること。
- `tools/` 配下のコードを一つでも変更した場合は、`.venv\Scripts\python.exe tools\run_tests.py` で全テストを実行すること。一部のテストだけで全テストを代替してはならない。対象を絞ったテストは全テストに加えて実行してよい。
- テストだけを目的とする操作が実行権限の不足で失敗した場合は、利用可能な権限昇格の仕組みを直ちに使用し、チャットで別途承認を求めないこと。ただし、実行環境自体が必須とする承認は省略できない。
- 完了を報告する前に最終差分を確認し、`git diff --check` を実行すること。変更内容に応じて `DEVELOPMENT.md` が求める追加の検証も行うこと。
