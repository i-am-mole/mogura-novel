# 静的サイト生成仕様

## 目的と適用範囲

この文書は `python tools/publish.py` で実行する静的サイトジェネレータ（SSG）の実装仕様を定める。
コンテンツの書式と編集方法は [CONTENT_GUIDE.md](CONTENT_GUIDE.md)、ブランチ運用と公開手順は [DEVELOPMENT.md](DEVELOPMENT.md) を参照すること。

SSG は `private/` を入力、`docs/` と `data/update_history.csv` を追跡対象の出力とする。名前が `-` で始まる作品ディレクトリと話Markdownは非公開入力として扱い、検証、集計、HTML生成、更新履歴更新の対象にしない。
全ページを毎回生成してよいが、同じ入力と更新履歴からは同じ追跡対象バイト列を生成しなければならない。
ファイルを生成したことと、コンテンツを更新したことは区別する。

## 生成物

SSG はサイト全体を生成し、次の追跡対象へ出力する。

| 出力 | 内容 |
| --- | --- |
| `docs/index.html` | 自己紹介と作品一覧を表示するサイトトップ |
| `docs/<work-slug>/index.html` | 作品情報と目次を表示する作品トップ |
| `docs/<work-slug>/<話順>.html` | `number` 順を1から数えた各話ページ |
| `docs/css/style.css` | `private/css/style.css` から生成するスタイル |
| `docs/CNAME`、favicon | `private/` に存在する対応ファイルのコピー |
| `docs/ogp/` | `private/ogp/` が存在する場合のコピー |
| `data/update_history.csv` | 入力ごとの意味内容ハッシュと更新日時 |

`docs/` と `data/update_history.csv` は SSG の管理対象であり、通常は直接編集しない。

## 用語

- **生成時刻**: SSGを実行した時刻。新規または変更された入力の更新日時を記録するときだけ使用する。
- **入力ファイル更新日時**: `data/update_history.csv` に記録する、個々の入力ファイル自身の意味内容が最後に変化した生成時刻。
- **話更新日時**: 話Markdownの入力ファイル更新日時。
- **作品更新日時**: 作品の `index.md` と、その作品に属する全話の入力ファイル更新日時の最大値。
- **サイト更新日時**: `private/self_intro.md` と全作品の作品更新日時の最大値。
- **表示更新日**: 対応する更新日時から取り出した暦日。時刻そのものはHTMLに表示しない。

単にSSGを実行しただけでは、いずれの更新日時も変えてはならない。
ファイルシステムの作成日時、更新日時（mtime）、列挙順、checkout時刻は更新判定にも並び順にも使用しない。

## 更新日時の表示

全ページの共通ヘッダーに表示する「更新」は、サイト更新日を意味する。

| ページ | 共通ヘッダーの表示更新日 |
| --- | --- |
| `docs/index.html` | サイト更新日 |
| `docs/<work-slug>/index.html` | サイト更新日 |
| `docs/<work-slug>/<話順>.html` | サイト更新日 |

ほかの表示箇所は次のとおりとする。

- サイトトップの各作品カードは、その作品の作品更新日を表示する。
- 作品トップの作品情報は、その作品の作品更新日を表示する。
- 作品トップの目次は、各話の話更新日を表示する。
- 話ページの話情報は、その話の話更新日を表示する。

生成時刻を全ページの共通ヘッダーへ一律に表示してはならず、更新履歴から
集約したサイト更新日を表示する。共通ヘッダー以外では、ある作品の更新を
別作品の作品更新日または話更新日として表示してはならない。

`data/update_history.csv` のタイムスタンプはタイムゾーン付きISO 8601形式で保存する。
SSGが新しく記録するタイムスタンプはUTCとし、秒までを保存する。
表示更新日は保存されたタイムスタンプのオフセットにおける暦日とするため、SSGが記録したUTCタイムスタンプはUTCの日付として表示する。

## 入力と生成物の依存関係

### 自己紹介

`private/self_intro.md` の意味内容が変化した場合は、次が変化する。

- `data/update_history.csv` の `private/self_intro.md` レコード
- `docs/index.html` の自己紹介とサイト更新表示
- サイトの表示更新日が変わる場合は、全HTMLの共通ヘッダー

作品の入力履歴、作品トップ、話ページは変化しない。
ただし、全HTMLの共通ヘッダーは上記のとおり変化し得る。

### 作品情報

`private/<work-slug>/index.md` の意味内容が変化した場合は、次が変化し得る。

- 当該 `index.md` の更新履歴レコード
- `docs/index.html` の当該作品カードとサイト更新表示
- `docs/<work-slug>/index.html`
- サイトの表示更新日が変わる場合は、全HTMLの共通ヘッダー

作品タイトルは話ページのHTMLタイトルとOGPタイトルにも使われるため、作品タイトルを変えた場合は当該作品の話ページも変化する。
話Markdownを変更していない限り、各話の話更新日時は変えない。

### 一話

`private/<work-slug>/<episode-slug>.md` の意味内容が変化した場合は、次が変化する。

- 当該話の更新履歴レコード
- 当該話に対応する `docs/<work-slug>/<話順>.html`
- 話更新日、タイトル、文字数等を掲載する `docs/<work-slug>/index.html`
- 作品更新日、合計文字数等を掲載する `docs/index.html`
- サイトの表示更新日が変わる場合は、全HTMLの共通ヘッダー

同じ作品の別の話の入力履歴と話更新日時、別作品の入力履歴とHTMLは変化しない。
ただし、別作品を含む全HTMLの共通ヘッダーは上記のとおり変化し得る。
ただし、話数番号の変更、話の追加・削除によって表示順、出力ファイル名、前後ナビゲーションが変わる場合は、その構造に依存する同作品内の話ページも変化し得る。

### 静的ファイルと生成ロジック

- `private/css/style.css`、CNAME、favicon、任意のOGP画像は対応する `docs/` 配下へコピーする。
- テンプレート、Markdown変換、ルビ変換、共通レイアウト等の生成ロジックを変えた場合は、そのロジックに依存する複数または全HTMLが変化し得る。
- 静的ファイルと生成ロジックの変更を、小説Markdown自身の更新日時として記録しない。

## 更新履歴

### ファイル形式

`data/update_history.csv` はヘッダーなし、3カラムのUTF-8 CSVである。

1. プロジェクトルートからの相対入力パス
2. 意味内容のSHA-256ハッシュ（小文字16進数）
3. タイムゾーン付きISO 8601更新日時

パス区切りはOSにかかわらず `/` とし、レコードはパスの昇順で保存する。
変更のないレコードはハッシュ、更新日時とも維持する。

### ハッシュ対象

ハッシュ対象は生成HTMLでも生のファイルバイト列でもなく、SSGが検証・解析した各入力ファイル自身の意味内容である。

- 話Markdown: 当該ファイルから解析した `title`、整数化した `number`、`content`
- 作品 `index.md`: 当該ファイルから解析した `title`、`tags`、`status`、`outline`、`external links`、`chapters`
- `self_intro.md`: 前後の空白を除去した自己紹介本文

作品 `index.md` のハッシュに各話のハッシュを含めてはならない。
`self_intro.md` のハッシュに作品または話のハッシュを含めてはならない。
作品更新日時とサイト更新日時は、個別レコードの更新日時を集約して求める。

キーが存在しない場合、または当該入力自身のハッシュが異なる場合だけ、そのレコードを生成時刻で更新する。
過去の複合ハッシュ形式から移行するときは、入力が変わっていないレコードのハッシュ表現だけを移行し、過去の更新日時を保持する。

### テキスト正規化

- 入力テキストはBOMなしUTF-8とする。
- `CRLF`、`LF`、`CR` は解析時に同じ改行として扱う。
- 最終行を終端する改行の有無だけでは、解析結果とハッシュを変えない。
- 空行、Markdown構造、値中の空白など、解析結果または生成HTMLを変える差は意味内容の差として扱う。
- 生成HTMLは既存の追跡ファイルがある場合、そのファイルの主要な改行形式を保持する。新規HTMLは `LF` とする。
- CSVはプラットフォームに依存しない一定の形式で書き出す。

したがって、Windowsとほかの環境で改行コードやパス区切りが異なるだけでは、入力を更新済みと判定せず、Git差分を発生させない。

## 作品一覧の順序

サイトトップの作品一覧は、次の優先順で決定する。

1. 作品更新日時の降順
2. 連載ステータス（`連載中`、`完結済`、`更新停止` の順）
3. 作品タイトルの昇順
4. `work-slug` の昇順

更新日時はISO 8601文字列の辞書順ではなく、タイムゾーンを考慮した時刻として比較する。

## 全ページ生成と冪等性

SSGは毎回サイト全体を一時ディレクトリへ生成し、検証と生成がすべて成功した場合だけ `docs/` と更新履歴を置き換えてよい。
全ページを生成することは、全ページの更新日またはバイト列を変えることを意味しない。

次を満たさなければならない。

- 変更なしで連続実行した2回目は、`docs/` と `data/update_history.csv` にGit差分を作らない。
- 同一の入力、更新履歴、生成ロジックからは、実行日、OS、パス表現、ファイルmtimeに依存しない結果を生成する。
- 内容が同一の生成物は、不要な空白、改行、時刻の埋め込み等で差分を作らない。
- 生成失敗時は既存の公開物と更新履歴を維持する。
- テストでは生成時刻を注入または固定でき、実行時の現在時刻に依存せず期待値を検証できる。

## 具体例: `return-of-son` の一話だけを修正

前提として、`private/return-of-son/abandonment.md` の本文中の一語だけを同じ文字数の語へ変更し、次のUTC日付にSSGを実行する。
その生成時刻は従来の作品更新日時とサイト更新日時より後であり、タイトル、話数番号、ほかの入力は変更しない。

Git差分が必要なファイルは次のとおりである。

- `private/return-of-son/abandonment.md`（編集者による変更）
- `data/update_history.csv`（`private/return-of-son/abandonment.md` のレコードだけ）
- `docs/return-of-son/1.html`（本文と話更新日）
- `docs/return-of-son/index.html`（目次の話更新日と作品更新日）
- `docs/index.html`（作品カードの作品更新日とサイト更新日）
- 上記を含む全HTML（共通ヘッダーのサイト更新日）

次のファイルまたはレコードは変化してはならない。

- `data/update_history.csv` の `private/return-of-son/index.md` レコード
- `data/update_history.csv` の `private/self_intro.md` レコード
- `docs/return-of-son/2.html` 以降の話ページの共通ヘッダー以外
- `docs/kaminomori/` 配下のHTMLの共通ヘッダー以外
- `docs/propeht-philina/` 配下のHTMLの共通ヘッダー以外

同じUTC日付内の変更ではサイトの表示更新日が変わらないため、無関係な作品の
HTMLは以前と同じバイト列になる。文字数など掲載メタデータも変化しない場合は、
対象作品トップまたはサイトトップにもGit差分がないことがあり、これは正常である。

## 必須回帰テスト

少なくとも次を自動テストする。

- 変更なしの連続生成がバイト単位で同一であること
- 固定した生成時刻で、一話変更時の生成物と履歴の差分範囲が上記具体例どおりであること
- サイトの表示更新日が変わる場合、全HTMLの共通ヘッダーへ反映されること
- 無関係な作品では、共通ヘッダー以外のHTMLが同一であること
- `self_intro.md` と作品 `index.md` の入力ハッシュ・更新日時が一話変更では変化しないこと
- 作品トップに対象話の更新日が反映されること
- `CRLF` と `LF`、Windows形式とPOSIX形式のパスが不要な差分を作らないこと
- 既存の入力検証、ルビ、Markdown、章、画像・CSSコピー、公開物の原子的置換が維持されること
